// src/theme/theme.ts
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#1976d2" },
  },
  components: {
    // tune MUI components if needed
  },
});

export default theme;
